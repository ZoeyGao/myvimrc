# vim settings

## Mac install vim-plug
```bash
curl -fLo ~/.vim/autoload/plug.vim --create-dirs \
    https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim
```
這樣再加入 vimrc vim-plug 後執行 :PlugInstall 才能安裝套件

# tmux settings
```bash
brew install tmux
```

# Other
```bash
brew install pipes-sh tty-clock
```

# pipes-sh
```bash
pipes.sh
```

# tty-clock
```bash
tty-clock -sc -C 2
```

# ---------- 碎碎念的 Changelog ---------- #
2026/05/12 14:23:43 - 本來是想存自己vimrc的設定，後來很少改，還是不太上手，又多用了tmux，可放一起的感覺

2026/05/12 14:41:35 - 還裝了一些自己喜好的東西像是...水管之類的
